---
interface Props {
  label: string;
  span?: "12" | "34";
}
const { label, span = "12" } = Astro.props;
const [labelCol, valueCol] = span.split("");
---

<li class={`c-grid c-grid-${span}`}>
  <h6 class={`c-grid-${labelCol}`}>{label}</h6>
  <p class={`c-grid-${valueCol}`}><slot /></p>
</li>

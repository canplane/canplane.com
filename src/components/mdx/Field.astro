---
interface Props {
  label: string;
  span?: "12" | "34";
}
const { label, span = "12" } = Astro.props;
const [labelCol, valueCol] = span.split("");
---

<li class={`grid grid-${span}`}>
  <h6 class={`grid-${labelCol}`}>{label}</h6>
  <p class={`grid-${valueCol}`}><slot /></p>
</li>

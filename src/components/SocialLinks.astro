---
import GitHubIcon from "./icons/GitHubIcon.astro";
import { Mail } from "@lucide/astro";

interface Props {
  links?: Record<string, string | undefined>;
}

const { links = {} } = Astro.props;
const entries = Object.entries(links).filter(([, value]) => Boolean(value));
const githubClass = "fill-primary-text h-7 w-7";
const emailClass = "stroke-primary-text h-7 w-7";
---

{entries.map(([key, value]) => (
  <a
    class="inline-flex items-center justify-center p-2 hover:opacity-50"
    title={key}
    href={key === "email" ? `mailto:${value}` : value}
    target={key === "email" ? undefined : "_blank"}
    rel={key === "email" ? undefined : "noopener noreferrer"}
  >
    {key === "github" && <GitHubIcon class={githubClass} />}
    {key === "email" && <Mail class={emailClass} stroke-width={1} />}
  </a>
))}

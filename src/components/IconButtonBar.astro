---
import GitHubIcon from "./icons/GitHubIcon.astro";
import EmailIcon from "./icons/EmailIcon.astro";

interface Props {
  links?: Record<string, string | undefined>;
  large?: boolean;
}

const { links = {}, large = false } = Astro.props;
const entries = Object.entries(links).filter(([, value]) => Boolean(value));
const iconClass = `fill-secondary-text ${large ? "h-[30px] w-[30px]" : "h-5 w-5"}`;
---

{entries.map(([key, value]) => (
  <a
    class="inline-flex items-center justify-center p-2"
    title={key}
    href={key === "email" ? `mailto:${value}` : value}
    target={key === "email" ? undefined : "_blank"}
    rel={key === "email" ? undefined : "noopener noreferrer"}
  >
    {key === "github" && <GitHubIcon class={iconClass} />}
    {key === "email" && <EmailIcon class={iconClass} />}
  </a>
))}
